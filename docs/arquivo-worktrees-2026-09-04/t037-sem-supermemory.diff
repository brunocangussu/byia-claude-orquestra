# Worktree t037-sem-supermemory — arquivado em 2026-09-04

## status
 M memory/MEMORY.md
 M memory/fixes-history.md
 M memory/wiki/KANBAN.md
 M memory/wiki/threads/T-037-sem-supermemory.md
 M memory/wiki/threads/T-043-protecao-contexto-codex.md
?? memory/wiki/threads/T-044-reset-concorrente.md

## branch
feat/t037-sem-supermemory
0ad1deb docs(0.22.3): registrar instalacao paritaria e smokes

## diff dos rastreados
diff --git a/memory/MEMORY.md b/memory/MEMORY.md
index 353a0a3..351cf35 100644
--- a/memory/MEMORY.md
+++ b/memory/MEMORY.md
@@ -10,6 +10,10 @@
 
 **⚡ ESTADO EM 2026-08-15 — leia isto primeiro:**
 
+- **VALIDAÇÃO PRÁTICA EM 2026-08-17:** o dono reabriu a task e pediu continuidade; o host carregou
+  Orquestra `0.22.3`, sem SuperMemory nas instruções ativas, e o prompt passou pelo guardião sem
+  bloqueio nem `/clear`. `T-037` e `T-043` foram fechados. `T-044` voltou a PLANNING para um plano
+  novo sobre a `0.22.3`; a candidata experimental `0.22.2` não será reaplicada.
 - **`T-037` em VALIDATE — `0.22.3` publicada e instalada:** arquitetura provider-neutral
   reconfirmada pelo dono; remoção do SuperMemory integrada com o guardião estável da T-043 e
   publicada em `origin/main` no commit `3bb1a24`. Codex e Claude estão na mesma `0.22.3`; Kimi recebeu
diff --git a/memory/fixes-history.md b/memory/fixes-history.md
index 72237b1..9c8f07f 100644
--- a/memory/fixes-history.md
+++ b/memory/fixes-history.md
@@ -1,5 +1,13 @@
 # Log de mudanças — append-only
 
+## [2026-08-17] validação+retomada | @frente-paridade-codex · 0.22.3 validada e T-044 reaberto
+
+O dono reabriu a task e pediu continuidade. O host carregou a skill Orquestra `0.22.3`, as
+instruções ativas não contêm SuperMemory/`sm-search`/`/orq:lembrar` e o prompt atravessou o
+guardião sem bloqueio nem exigência de `/clear`. Isso fechou a validação prática de `T-037` e
+`T-043`. `T-044` voltou a PLANNING, mas a candidata experimental `0.22.2` não será reaplicada:
+o plano será regenerado sobre `origin/main`/`0.22.3` antes de qualquer código.
+
 ## [2026-08-15] release+instalação | @frente-supermemory · 0.22.3 provider-neutral nos três hosts
 
 A release combinada T-037/T-043 foi aprovada, publicada em `origin/main` no commit `3bb1a24` e
diff --git a/memory/wiki/KANBAN.md b/memory/wiki/KANBAN.md
index 0be807b..64ec435 100644
--- a/memory/wiki/KANBAN.md
+++ b/memory/wiki/KANBAN.md
@@ -19,6 +19,8 @@
 
 ## 🟡 Fazendo
 
+- [>] `T-044` Endurecer reset concorrente do guardião de contexto — reaberto em 2026-08-17 para replanejamento sobre a `0.22.3` publicada. A candidata experimental `0.22.2` não será reaplicada: é preciso reproduzir novamente a corrida `clear × transação` e revisar `PreCompact`/`PostCompact` na fonte atual antes de um novo gate. Nenhuma implementação iniciada. @frente-protecao-contexto
+
 - [~] `T-040` Paridade operacional do Orquestra no Codex — coordena `T-041` (core `0.21.0`), `T-043` (guardião `0.22.3`, agora em VALIDATE) e `T-042` (statusline opt-in `0.23.0`). A `0.22.3` foi publicada e instalada com paridade em Codex, Claude e Kimi; esta frente continua aberta para a validação prática do dono e a futura `T-042`. @frente-paridade-codex
 - [?] `T-041` Paridade core do Codex — `0.21.0` instalada e validada em Claude/Codex, caches idênticos e smoke externo com `OPUS_MODEL=claude-opus-5` + Kimi exit 0. Push autorizado e confirmado no GitHub: `main` contém a versão e o runner. **Aguarda validação prática do dono no projeto real que antes falhava, em nova sessão Codex.** @frente-paridade-codex
 
@@ -29,9 +31,6 @@
 
 ## 🟣 Validar
 
-- [?] `T-037` Tirar o SuperMemory do sistema de desenvolvimento — **`0.22.3` PUBLICADA E INSTALADA**: `origin/main` em `3bb1a24`, Codex e Claude na mesma versão e Kimi no mesmo snapshot. Não há SuperMemory, `sm-search` ou `/orq:lembrar` ativos no produto nem nas instruções globais; resíduos foram movidos para backup recuperável em `~/.codex/backups/orquestra-0.22.3-b84bc51`. Suíte de 113 testes, gates, comparação dos caches e smokes em processos novos passaram. A task atual ainda carregava o hook `0.22.2`; o cache antigo foi restaurado sem tocar na `0.22.3`. Aguarda validação prática do dono após reabrir a task; T-044 permanece fora. @frente-supermemory
-- [?] `T-043` Proteção preventiva da janela de contexto no Codex — guardião consultivo publicado na `0.22.3`: Codex não bloqueia, recuperação sem telemetria e rearme em +10 p.p.; Claude preserva `/clear`. Suíte combinada de 113 testes, parecer final aprovado, smoke Codex consultivo e smoke Claude-only fail-open passaram. Aguarda validação prática do dono em task reaberta. O aviso global `_managedBy` é preexistente e segue separado da release. @frente-protecao-contexto
-
 - [?] `T-036` O `/orq:init` apaga a statusline do dono — a checagem "não sobrescreva" olha o escopo errado — 🔴 **queimou de verdade em 2026-08-07**, no projeto `IVA - App System`. O `init.md:195-197` manda apontar o `statusLine` para o `kanban-status.sh` e ressalva *"se já houver statusline customizada, **não sobrescreva**"* — **mas não diz em que escopo procurar**. A statusline do dono vive no **settings global** (`~/.claude/settings.json` → `~/.claude/statusline.sh`, 99 linhas: modelo, effort, contexto, custo, rate limit 5h com barra, diretório, worktree, branch com staged/modified, **e o kanban**). O `init` olhou o `.claude/settings.json` **do projeto**, achou vazio de `statusLine`, concluiu "não há customizada" e gravou a chave — e settings de projeto **vencem o global por precedência**. Efeito: naquele projeto a barra virou só `📋 0% (0/6) ⏳1`, perdendo tudo o mais; e o dono lê isso como "o plugin quebrou minha configuração global", que é o que parece. **Nada foi sobrescrito literalmente** — o diff do `settings.json` do IVA é `+4 -0`, aditivo — o que torna o defeito invisível a qualquer verificação que compare arquivos. **Segundo defeito, no mesmo lugar:** o comando gravado é `sh "$HOME/Projetos DEV - Cursor/byia-claude-orquestra/orq/scripts/kanban-status.sh"` — **caminho absoluto para o repo de desenvolvimento do plugin**, não `${CLAUDE_PLUGIN_ROOT}` nem `~/.claude/scripts/`. Renomear ou mover este repo quebra a statusline de todo projeto que passou pelo `init` (é a mesma família do `T-029`). **Terceiro:** mesmo com a checagem consertada, apontar o `statusLine` do projeto para o kanban **puro** é sempre perda quando a statusline do host já integra o board — a global dele já chama `~/.claude/scripts/kanban-status.sh` e concatena. **Correção proposta:** (a) checar `statusLine` em **todos** os escopos (global, projeto, local) antes de decidir, e tratar *"existe no global"* como *"já há customizada"*; (b) quando houver, **compor, nunca substituir** — mostrar a linha a acrescentar ao script existente, como o próprio texto já promete; (c) trocar o caminho hardcoded por `${CLAUDE_PLUGIN_ROOT}/scripts/kanban-status.sh`; (d) detectar que a statusline existente **já chama** o `kanban-status.sh` e, nesse caso, não propor nada. **Já revertido no IVA** (chave removida do `.claude/settings.json`, arquivo volta ao estado do commit `1fd66fe`) — o card é sobre o plugin, não sobre aquele projeto. **Plano v1 em `memory/wiki/threads/T-036-statusline.md`** (2026-08-07). ⚠️ **ESCOPO AMPLIADO PELO DONO em 2026-08-07, após o plano v1** — ele aprovou as seis recomendações **e** pediu mais: *"se você conseguir incluir ela [a statusline global dele] nesse plugin de forma geral, que quando instalasse já instalasse ela e a nossa statusline desse projeto, ficar uma coisa só"* + *"quero que a statusline de todos os projetos no meu computador fique exatamente da forma mais completa que eu já tinha"*. Isso vira **feature, não só conserto**: o plugin passa a **distribuir** a statusline completa (modelo · effort · contexto · custo · rate-limit 5h · dir · worktree · branch · board), e o `init` a instala em vez de só apontar para o kanban. As regras R1-R4 do plano v1 continuam valendo — a feature **grava** statusLine, então precisa mais ainda das regras de escopo e precedência. Replanejamento pedido ao mesmo Planner. **Varredura ampla refeita (todo o `~`, 6 níveis): eram DOIS projetos afetados** — `IVA - App System` e `Prompts - Byia/prompts-byia-clientes` (neste a chave estava **commitada**, em `fa1363c`); os dois já revertidos à mão pelo Manager. A primeira varredura só desceu 1 nível e por isso disse "só o IVA" — o dono estava certo ao falar em "projetos". @frente-statusline ⏸️ **PERGUNTA AO DONO (v2, 2026-08-07):** decisões 1-6 já aprovadas; faltam as 7-9 — **(7)** em máquina sem statusline nenhuma, o init deve oferecer instalar para **todos os projetos** (grava `~/.claude/`) ou só no projeto? (rec.: oferecer, default projeto); **(8)** "uma coisa só" = par `statusline.sh`+`kanban-status.sh` instalado junto, não arquivo único? (rec.: par); **(9)** sem `jq`, degradar para só-board e nunca instalar jq sozinho? (rec.: sim). **+ pendência fora deste repo:** commitar a reversão no `prompts-byia-clientes`, onde a chave defeituosa está em `fa1363c` — sem commit ela volta num checkout. 🔄 **REPLANEJAMENTO v3 em curso (2026-08-07, fim do dia).** A v2 foi ao painel **2 rodadas** e reprovou **5 de 5** — a 2ª achou bloqueadores criados pelas correções da 1ª, todos em **interseções de ramos**. Levei ao dono duas saídas (simplificar para "nunca escreve, só mostra" · manter edição automática) e **ele recusou as duas com argumento**: *"O ideal seria que o script pesquisasse se já existisse uma board, ver qual é a arquitetura dessa board, e incluir nela a parte das tasks"* — compor **dentro** da barra existente, que é operação que o produto nunca fez (o dano do incidente veio da **chave no settings**, não de editar script; premissa corrigida com ele). **Cinco travas prometidas:** backup carimbado · idempotência · achar o ponto de inserção · validar depois (sintaxe + execução antes×depois, sem perder segmento) · reverter sozinho e cair para "mostrar" com o motivo. **Pergunta central da v3:** como o init prova que entendeu a arquitetura de um script que nunca viu, **antes** de escrever nele — e como reconhece que NÃO entendeu, a tempo. Implementação da v2 segue no working tree, não commitada; a v3 decide o que aproveita. ✅ **PLANO v3 PRONTO e DECISÕES 10-12 APROVADAS pelo dono em 2026-08-08** ("continue"): compor com alvo fora do projeto mediante aprovação nominal · estratégia **E1 bloco-sufixo** (não edita linha alheia; validação por **prefixo byte-a-byte**) · ramo "sem `jq`" **morto**. Confirmado ao dono que o init **pergunta antes de compor** (ele aprova; nós aplicamos). As decisões 7-8 já estavam aprovadas desde 07/ago — o plano as re-listou por dúvida, corrigido no gate. A pendência "confirmar reversão commitada no prompts-byia-clientes" está **RESOLVIDA**: commit `41fa1f9`. **ESCOPO REDUZIDO em 2026-08-08, decisão do dono** (*"siga sua recomendação"*) após **3 rodadas de painel, 8 pareceres, 8 reprovações**. Fica **neste card**: o conserto do bug (nunca gravar chave havendo statusline em qualquer escopo), o asset `orq/scripts/statusline.sh` com os defeitos graves fechados (guarda `[ -r ]` — antes zerava a barra sem `jq`; injeção de código no `awk` do custo), as folhas **F1** (instalar em máquina limpa) · **F2** (consertar instalação nossa antiga) · **F3** (já mostra o board → não mexer), o tripwire do lint e o bump 0.20.0. **SAI para o `T-038`:** a folha **F4** (compor dentro de barra alheia) e todo o `orq/compor-statusline.md`. **Razão:** o conserto está maduro e resolve o problema real do dono; a composição é que consumia rodada após rodada, e **na máquina dele ela nunca dispara** (a barra dele já mostra o board → F3). ✅ **RECORTE EXECUTADO em 2026-08-08.** A árvore virou **três folhas** (F1 instalar · F2 consertar instalação nossa · F3 tudo o mais), com **um único predicado binário**: o alvo diretamente invocado aponta para dentro do plugin? Sim → F2; qualquer outra coisa → F3. Dentro de F3, um segundo teste decide só **o que relatar** ("já mostra o board" · "não mostra, aqui está o bloco") — **nunca abre folha nova e nunca escreve**. Script inexistente, ilegível, comando inline ou statusline noutra linguagem: tudo cai em F3 e recebe o mesmo relato, **sem exigir que o init entenda a arquitetura de script alheio**. R5 removida (só existia para F4); R4 caiu de três ramos para dois. `orq/compor-statusline.md` **saiu do produto** → `memory/wiki/threads/T-038-compor-statusline.md`, com o achado de segurança nomeado no cabeçalho e **não corrigido** (é do `T-038`). Motivo de mover em vez de descitar: arquivo dentro de `orq/` é **distribuído**, e um modelo pode lê-lo e segui-lo sem citação. Gates verdes; `grep -rn "compor-statusline\|F4" orq/` **vazio**. ⚠️ **Efeito colateral aceito:** a antiga Decisão 6 (aplicar a composição em script dentro do projeto, com aprovação) **caiu junto** — sem F4 não há edição de script nenhuma. Se o dono quiser de volta, é o `T-038`. **Falta: painel final + release.** 🚀 **RELEASE 0.20.0 PUBLICADO em 2026-08-09** — commit `164387c`, push `b62b39c..164387c`, marketplace e plugin atualizados no **Claude** (cache `diff -rq` vazio) e no **Codex** (cache velho de 08/ago 13:47 removido e reinstalado — estava rotulado 0.20.0 com conteúdo pré-correções; **gotcha novo: o marketplace do Codex aponta para a pasta do projeto, então instalar copia o disco, inclusive trabalho não commitado**). **AGUARDA O TESTE DO DONO:** projeto de rascunho vazio → `/orq:init` → tem que (a) relatar *"sua statusline já mostra o board"* e (b) **não gravar chave nenhuma** (`jq .statusLine .claude/settings*.json` → null nos dois). Se ele oferecer instalar statusline ou tocar em settings, **reprovou**.
 - [?] `T-015` Diagnóstico de ambiente dá **falso all-clear** — 🔴 **voltou de VALIDATE: reprovado por 3/3 revisores** (Claude+Codex+Kimi, 2026-07-28). O card afirmava "atritos diagnosticáveis" e o diagnóstico erra no primeiro item. **Confirmado empiricamente:** (a) checagem de plugin desatualizado compara *versão*, mas o cache é indexado por versão — `diff -rq ~/.claude/plugins/cache/orquestra/orq/0.10.0/ ./orq/` acusa `lint-coerencia.py` divergente **agora**, e o diagnóstico diz "tudo certo"; (b) "board legível" só manda rodar o script — 7 cards escritos, 4 com marcador em negrito → `📋 33% (1/3)` sem `⚠`, reportaria ✓; (c) `README.md:389` ("marcador ou ID **dentro de** negrito/crase não casa") contradiz `_schema.md:15` e o exemplo logo acima — quem seguir tira as crases do ID e perde o board inteiro; (d) `/reload-plugins` vs restart contradiz `distribuicao.md:26` e o `--help` da CLI; (e) `_schema.md` ausente acusado como defeito, mas `checkpoint` e `wiki-lint` degradam de boa; (f) fallback de caminho conhecido existe só pro Kimi, não pro Codex. **Implementado e revisado em 2026-07-29** (Fable planejou · Sonnet implementou · Claude+Codex+Kimi revisaram · 7 achados aplicados). **Como validar (pós-release + restart):** rode `/orq:stack --verificar` e confira que ele compara **conteúdo** do cache (`diff -rq`), não só a versão; que "board legível" mostra os três sinais; e que num projeto sem `memory/` ele **não** acusa `_schema.md` como defeito nem propõe criar a árvore.
 - [?] `T-016` Colisão de roteamento na skill — 🔴 regressão do `T-014` (que está em VALIDATE). `SKILL.md:63` roteia *"não funciona"* para o **ciclo**; `SKILL.md:74` roteia *"não está funcionando"* para o **diagnóstico**. Mesma frase, dois destinos, sem desempate. Cenário: *"o painel de revisão não está funcionando"* sobre bug real → roda diagnóstico, responde "ambiente ok", **o bug nunca vira card**. Junto: `SKILL.md:70` ainda descreve o painel como "Claude + Codex" — o Kimi entrou na 0.8.0. **Implementado e revisado em 2026-07-29** (Fable planejou · Sonnet implementou · Claude+Codex+Kimi revisaram · 7 achados aplicados). **Como validar (pós-release + restart):** diga *"o painel de revisão não está funcionando"* — tem que **criar card e planejar**, não encerrar em "ambiente ok". Depois diga *"o Kimi sumiu do PATH"* — tem que ser diagnóstico, sem card. O segundo é o controle: se os dois virarem card, o desempate ficou frouxo. **✅ TESTE 1 DE 3 PASSOU (2026-07-29, contra a 0.11.0 já ativa na sessão):** o dono disse *"queria melhorar o relatório do checkpoint"* e o comportamento foi anunciar o roteamento, criar o card (`T-022`), **não tocar em arquivo do produto** e parar no gate. **✅ TESTE 2 DE 3 PASSOU (2026-07-29):** o dono disse *"o painel de revisão não está funcionando"* e o comportamento foi invocar a skill **antes de abrir o board**, aplicar o desempate e **criar card (`T-024`) roteando pelo ciclo** — nada de encerrar em "ambiente ok". **✅ TESTE 3 DE 3 — O CONTROLE — PASSOU (2026-07-29):** *"o Kimi sumiu do PATH"* virou **diagnóstico, sem card**, e a checagem não parou no `which`: o binário está em `~/.kimi-code/bin/kimi` (v0.29.2, 152 MB) e o PATH o alcança por symlink em `~/.local/bin/kimi` (mesmo sha256), OAuth renovado no mesmo dia. Ambiente ok, nada criado no board. **Os dois lados do desempate se comportaram diferente, que era exatamente o ponto** — um virou card, o outro não. Os três testes estão cumpridos; dá para fechar se você concordar. **Só o dono pode rodar estes testes** — o Manager sabe o que a instrução manda e acertaria de memória; aqui o viés é explícito, já que a frase do teste está escrita neste próprio card.
@@ -48,8 +47,6 @@
 
 ## 🔵 Backlog
 
-- [ ] `T-044` Endurecer reset concorrente do guardião de contexto — Opus 5 e Kimi K3 concordaram que uma transação anterior pode apagar o marcador `.reset` criado por `SessionStart(clear)`, causando over-enforcement recuperável (pedir um segundo `/clear`), e que o fallback Windows `lockdir` não recupera lock órfão. Follow-up da revisão final do `T-043`; sem bypass silencioso no alvo macOS/Linux. @frente-protecao-contexto
-
 - [ ] `T-042` Statusline nativa do Codex — perfil opt-in, backup/merge/rollback e limite explícito sem board em release local `0.23.0`; plano em `docs/superpowers/plans/2026-08-09-statusline-nativa-codex.md`. Executar somente depois de `T-043`. @frente-paridade-codex
 
 - [ ] `T-032` O protocolo de várias janelas ficou incompleto onde o projeto cresceu depois dele — 🟡 pedido do dono em 2026-08-05, a partir da pergunta *"como está a questão do gerenciamento dos checkpoints em múltiplas janelas do mesmo repositório?"*. **O `T-013` funciona** — foi validado hoje **entre hosts diferentes** (o Codex detectou uma edição do Manager em `gotchas.md`, reconheceu que não era da frente dele e excluiu do escopo sem sobrescrever). **Mas três buracos apareceram ao responder a pergunta, e dois são erro do Manager:** **(1) o log tem duas instruções contraditórias** — `memory/wiki/_schema.md` manda *"append no **fim**, entrada carimbada com a frente"* e `orq/commands/checkpoint.md` manda *"append no **TOPO**"*. É a mesma família de defeito que a 0.17.0 inteira combateu (regra em dois lugares, uma falsa), e estava aqui desde antes. **(2) nenhuma entrada de log do Manager é carimbada com a frente**, embora o schema exija — com três janelas escrevendo, daqui a um mês não se sabe quem fez o quê. **(3) a seção `⏸️ O que espera o dono` do `memory/MEMORY.md` nasceu em 2026-08-04 sem regra de concorrência** — o schema não a lista na tabela de "quem escreve o quê", então **duas janelas dando checkpoint se sobrescrevem ali em silêncio**, e é justamente a seção que o dono lê primeiro ao voltar. **Nenhuma quebrou nada ainda** — a (3) só morde com dois checkpoints quase simultâneos. **Decidir também:** o append no fim vs. topo tem o mesmo ponto de disputa nos dois casos; talvez a saída seja outra (uma seção por frente? arquivo por frente?) em vez de escolher entre os dois. **Não confundir com o `T-013`**, que está em VALIDATE e cobre o board — este é sobre **log, índice e a marca de frente**.
@@ -79,6 +76,9 @@
 
 ## ✅ Feito
 
+- [x] `T-037` Tirar o SuperMemory do sistema de desenvolvimento — validado pelo dono em task reaberta em 2026-08-17: o host carregou Orquestra `0.22.3`, as instruções ativas não contêm SuperMemory/`sm-search`/`/orq:lembrar` e a retomada funcionou normalmente. Release, instalação paritária e suíte de 113 testes permanecem como evidência técnica. @frente-supermemory
+- [x] `T-043` Proteção preventiva da janela de contexto no Codex — validado pelo dono em task reaberta em 2026-08-17: o prompt “continue” atravessou o guardião `0.22.3` sem bloqueio e sem exigir `/clear`. Claude conserva seu contrato separado; `_managedBy` permanece follow-up independente. @frente-protecao-contexto
+
 - [x] `T-024` O painel de revisão não está funcionando — ⚠️ **nasceu de uma frase de teste de roteamento (`T-014`/`T-016`), não de uma falha observada** — confirmar com o dono antes de planejar: se foi só o teste, este card fecha sem trabalho nenhum. Mantido no board de propósito, porque engolir o card é justamente o defeito que o desempate existe para evitar. 🟡 relato do dono em 2026-07-29, **sem sintoma detalhado ainda**: falta saber se não roda, se roda e volta vazio, se só um parecer volta, ou se trava. Entrou pelo **ciclo** e não pelo diagnóstico de ambiente porque o painel é o que o **produto** faz (`/orq:revisar`) — pelo desempate da skill, o card nasce antes da investigação, e o diagnóstico, se rodar, serve ao plano. **Cruzamento obrigatório:** `T-010` (painel consertado — duas causas raiz: `codex exec` bloqueia lendo stdin sem TTY, resolvido com `< /dev/null`; e subagente spawnado **com `name`** vira teammate e nunca devolve resultado) e `T-007` (Kimi em `~/.kimi-code/bin/kimi`, fora do PATH) estão **os dois em VALIDATE, nunca validados pelo dono na prática** — se a falha se confirmar, os dois voltam reprovados e a causa raiz é provavelmente regressão de um deles, não defeito novo. **Terceira hipótese, com precedente:** em 2026-07-28 os pareceres de Codex e Kimi **estouraram o tempo** no meio de uma revisão real e voltaram parciais, sem veredito. **Próximo passo:** planner reproduz `/orq:revisar` numa mudança pequena e localiza onde quebra — invocação, execução do CLI externo, retorno do subagente ou reconciliação. **✅ FECHADO EM 2026-07-31 SEM TRABALHO — o dono confirmou que era a frase de teste**, o painel não falhou. O card existiu de propósito: engoli-lo teria sido o defeito que o desempate do `T-016` existe para evitar. **Fica pendente em outro lugar:** o painel continua sem validação prática do dono — isso é o `T-010`, em VALIDATE.
 - [x] `T-003` Piloto end-to-end — **cumprido em 2026-07-27, e melhor do que o card pedia:** o `/orq:init` rodou em repositório de terceiro, por outra LLM, sem eu por perto. O relatório de atrito veio com 10 achados (4 bugs de contrato reais) e virou o `T-011`. Também validou o que **funciona**: a regra "este comando se ADAPTA ao projeto" cortou 6 agentes genéricos que seriam criados sem ela, e o `_stack.md` com "Dispensadas" evitou repropor ferramenta já recusada. **Como validar:** nada a testar — o card era o próprio experimento. Feche se concordar que o piloto respondeu a pergunta. **Fechado em 2026-07-29:** o piloto respondeu a pergunta.
 - [x] `T-008` Lint de coerência interna — `orq/scripts/lint-coerencia.py`. Confere `/orq:x`, `` `orq-agente` ``, `` skill `nome` `` e `${CLAUDE_PLUGIN_ROOT}/arquivo` contra o que existe; ignora `memory/`. Testado nos dois sentidos: passa no estado atual e pega os 4 tipos de defeito quando injetados. **Como validar:** renomeie mentalmente um comando (ou rode `python3 orq/scripts/lint-coerencia.py .` depois de editar qualquer coisa) e veja se ele aponta `arquivo:linha`. Documentado no `CLAUDE.md` como verificação obrigatória junto do `validate`. **Fechado em 2026-07-29:** validado injetando os 4 tipos de defeito numa cópia — pegou os 4 com `arquivo:linha` e `exit=1`.
diff --git a/memory/wiki/threads/T-037-sem-supermemory.md b/memory/wiki/threads/T-037-sem-supermemory.md
index 769a870..7af3cc3 100644
--- a/memory/wiki/threads/T-037-sem-supermemory.md
+++ b/memory/wiki/threads/T-037-sem-supermemory.md
@@ -816,3 +816,9 @@ reabertura continuam posteriores à publicação e aos smokes novos em Codex e C
 
 T-037 está em VALIDATE. O único gate restante é o dono reabrir a task, usar o Orquestra normalmente
 e confirmar o comportamento prático; só essa confirmação move o card para DONE. T-044 continua fora.
+
+## Validação prática do dono — 2026-08-17
+
+A task foi reaberta com Orquestra `0.22.3`; as instruções ativas não contêm SuperMemory,
+`sm-search` ou `/orq:lembrar`, e a continuidade funcionou normalmente. O gate prático foi
+cumprido e `T-037` foi movido para DONE.
diff --git a/memory/wiki/threads/T-043-protecao-contexto-codex.md b/memory/wiki/threads/T-043-protecao-contexto-codex.md
index 32b844b..8fa7c81 100644
--- a/memory/wiki/threads/T-043-protecao-contexto-codex.md
+++ b/memory/wiki/threads/T-043-protecao-contexto-codex.md
@@ -272,3 +272,9 @@ stdout nem estado e o contrato `/clear` permaneceu. Os caches novos bateram com
 
 ⏭️ RETOMAR AQUI — VALIDATE: o dono deve reabrir a task e confirmar o comportamento real. O warning
 global `_managedBy` é preexistente e continua em follow-up separado; não bloqueia esta validação.
+
+## Validação prática do dono — 2026-08-17
+
+A task reaberta carregou o guardião Orquestra `0.22.3` e aceitou o prompt “continue” sem bloquear
+a mensagem e sem exigir `/clear`. O gate prático foi cumprido e `T-043` foi movido para DONE.
+O warning `_managedBy` continua separado e não altera este resultado.
