# T-053 — Incorporar o Fable 5.1 no elenco

**Pedido do dono em 2026-09-01:** *"vamos mudar para o Fable 5.1, que acabou de ser lançado…"*

Thread criada em 2026-09-02 pela frente `@frente-economia`, para receber a nota que vivia na linha
do card quando o board ganhou teto. O conteúdo abaixo é o texto original, íntegro.

## ⏭️ RETOMAR AQUI

`claude update` para 2.1.255+, reiniciar, e reler `additionalModelOptionsCache` — só o cache
pós-update diz se o alias `fable` passa a resolver 5.1 sozinho ou se o 5.1 tem alias próprio.

## Nota herdada do card `T-053` (migrada em 2026-09-01)

> Texto **íntegro** que vivia na linha do card, movido para cá quando o board ganhou
> teto de 200 chars. Nada foi resumido nem descartado — só realocado.

**Título na época:** Incorporar o Fable 5.1 no elenco

🟡 **pedido do dono em 2026-09-01**: *"vamos mudar para o Fable 5.1, que acabou de ser lançado… inclusive parece ser mais barato que o Fable 5 e mais efetivo"*. ⚠️ **NÃO comprovado nesta sessão, e por isso virou card em vez de edição:** a referência de modelos disponível (cache 2026-06-24) lista `claude-fable-5` a **$10/$50 por MTok** — mais caro que o Opus 5 ($5/$25) — e **não menciona** um 5.1; o ambiente desta sessão também declara apenas `claude-fable-5`. O CLI `ant` não está instalado, então não deu para consultar a Models API. **A hipótese que muda tudo:** o Orquestra **nunca grava ID versionado** — o elenco usa o **alias `fable`**, porque o subagente do Claude Code só aceita `opus`/`sonnet`/`haiku`/`fable`/`inherit`. Se a Anthropic lançou o 5.1, **o alias provavelmente já resolve para ele** e não há nada a mudar no produto. ✅ **RESOLVIDO SEM MUDANÇA NO PRODUTO — 2026-09-01, depois de o dono atualizar o CLI.** Estado verificado no `~/.claude.json` às 19:38: CLI **2.1.258**, e o `additionalModelOptionsCache` tem **uma única entrada** — `{"value": "**claude-fable-5-1[1m]**", "label": "**Fable**", "description": "**Fable 5.1** · Most capable…"}`. A entrada `disabled` sumiu. **O alias `fable` resolve para o Fable 5.1**, e como o elenco grava o alias (`_elenco.md:145`, `:236`) e nunca ID versionado, o `planner·interface` **já usa o 5.1 automaticamente, nas duas versões do produto (0.24.0 e a do GitHub)**. Isso é a prova prática da regra: gravar `claude-fable-5` teria congelado o time na versão anterior. **Sobra só:** confirmar o preço do 5.1 e, **se ele deixou de ser o degrau mais caro**, revisitar a nota de custo do `planner·interface` e do preset `economia`, que hoje presumem o Fable como o mais caro do elenco ($10/$50, o dobro do Opus 5) — se inverteu, a lógica do `economia` muda. Card pode ir a VALIDATE assim que o preço for confirmado. ‖ **Histórico — o que se sabia antes do update (mantido porque a lição de método vale):** o cache trazia `{"value": "cc-update-required-1", "label": "Fable 5.1 (disabled)", "description": "Update to 2.1.255+ to use Fable 5.1", "disabled": true}` com CLI 2.1.246 — o item aparecia no menu e **estava indisponível**; o `value` era sentinela de update, não ID. ⚠️ **Erro de método do Manager, registrado de propósito:** concluí *"se o 5.1 existe, o alias já o usa"* a partir do texto do `claude --help` **sem conferir o cache local**, e o primeiro grep que fiz "confirmando" casou com os **transcripts desta própria conversa**. A regra do alias estava certa; a afirmação sobre *aquela máquina naquele momento* estava errada. ‖ **Contexto original:** O `~/.claude.json` (`additionalModelOptionsCache`) traz **duas** entradas: `{"value": "claude-fable-5[1m]", "label": "Fable", "description": "Fable 5 · Most capable…"}` e `{"value": "cc-update-required-1", "label": "**Fable 5.1 (disabled)**", "description": "**Update to 2.1.255+ to use Fable 5.1**", "disabled": true}`. O CLI instalado é **2.1.246**. Ou seja: **o Fable 5.1 existe, aparece no menu, e está indisponível nesta máquina** — o `value` nem é ID de modelo, é sentinela de update. O alias `fable` resolve hoje para **Fable 5**, não 5.1. ⚠️ **Erro de método do Manager, registrado de propósito:** eu havia concluído *"se o 5.1 existe, o alias já o usa"* raciocinando a partir do texto do `claude --help` (*"alias for the latest model"*) **sem conferir o cache local** — e o primeiro grep que fiz "confirmando" o 5.1 casou com os **transcripts desta própria conversa**, não com evidência independente. A regra do alias vale, mas só entre os modelos que a instalação alcança. **Próximo passo: `claude update` para 2.1.255+, reiniciar, e reler `additionalModelOptionsCache`** — só o cache pós-update diz se o alias `fable` passa a resolver 5.1 sozinho ou se o 5.1 tem alias próprio. **Não presumir de novo.** ‖ **Antes era (mantido como registro do que se sabia):** `claude --help` documenta que `--model` aceita *"an alias for the **latest model** (e.g. 'fable', 'opus', or 'sonnet') **or** a model's full name (e.g. 'claude-fable-5')"*. O alias aponta para o **mais recente da linha, por definição** — e o Orquestra grava o alias em toda parte, tanto na `0.24.0` quanto no ramo remoto (`_elenco.md:31`, `:37`). **Conclusão: se o Fable 5.1 existe, o elenco já o usa, sem mudança nenhuma no produto.** **Sobra pouco trabalho, e nenhum dele é urgente:** (1) confirmar existência e preço (Models API com o CLI `ant`, que **não está instalado** aqui, ou o dono conferindo na interface); (2) **se o preço caiu de fato**, revisitar a nota de custo do `planner·interface` e do preset `economia`, que hoje presumem o Fable como o degrau **mais caro** do elenco ($10/$50, o dobro do Opus 5) — se ele ficou mais barato que o Opus, a lógica do `economia` inverte e vale reexaminar; (3) **não** gravar ID versionado no elenco: seria reintroduzir o defeito `T-051`/B2 (elenco declara um modelo e a via executa outro) e ainda congelaria o time numa versão, perdendo justamente o benefício do alias. @frente-elenco

